# static size of image regardless of height and width ratio
# image size does not fit all images (reshapes images -> should hold one side).

# card is not a vue component

# masks duplicated in python and js

# image size is duplicated in python and HTML (there it is duplicated again).

# image directory is duplicated in python and js

# file image changes -> old image not removed!
